# Frontend

This folder contains the React + Vite frontend for the student task manager.

## Local development

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` in `.env` to the deployed API Gateway URL.

## Features

- Add task
- View tasks
- Mark task complete
- Delete task
- Basic loading and error states
